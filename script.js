//goal is to have as little global code as possible. Going hard on encapsulation
//If only need one instance of something, warp it in an IIFE/module pattern so that it cannot be reused
//Odin advises completing game in console before worrying about DOM at all
function gameboard(){
    gameboard = []; //holds array of squares/spots
    //3x3
}
function player(){
    //x player or o player
    //my turn or not my turn
    //place x/o command

}
function gameController(){
    //win condition checker
    //whose turn is it
    //thats it(?)
}